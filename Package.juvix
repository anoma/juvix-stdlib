module Package;

import PackageDescription.V2 open;

package : Package :=
  defaultPackage@{
    name := "stdlib";
    version :=
      mkVersion@{
        major := 0;
        minor := 12;
        patch := 1;
        release := nothing;
      };
    dependencies := [];
  };
